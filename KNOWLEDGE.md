# ノウハウ台帳

このファイルは「設計原則（持ち越し資産）」と「見つけたバグのクラス → 恒久対策」を
蓄積する台帳。**新種のバグを見つけたら必ずここに追記し、可能な限り自動検証
（テスト・invariant・シナリオのアサーション）に落とすこと。台帳に書くだけで終わらせない。**

下の設計原則は、前作群（ゲーム開発の系譜: proto_ae → html-tap-adventure-template →
html-game-template）で実際に踏んだバグ・ユーザー指摘から、**開発一般に通じる形に
一般化して**持ち越したもの。領域固有の原則（ゲームUI・遊びのデザイン等）は各ドメイン
テンプレート側の台帳にあり、ここには持ち込まない。**このプロジェクトで新しく学んだ
ことは、原則なら「設計原則」へ、個別バグなら「バグ台帳」へ足す。**

## 設計原則（開発一般）

### 価値の原則（全原則に優先）

0. **測れないものは設計工程で守る（コア価値ゲート）** — 検証の緑は正しさの証明ではない。
   到達可能性・回帰・遮蔽は測れるが、**コアの価値（そのプロダクトが与える体験・解く問題）は
   測れない**。測れるものだけが最適化される——だからコアの価値は検証ではなく設計工程で守る。
   - 機能を足す・直す**設計段階**で必ず問う：「この変更はコアの価値に向かっているか？
     汎用の道具・ありふれた実装に差し替えても同じなら、コアの語彙で言い換えられないか？」
   - **ユーザー／テスターは摩擦（詰まる・分かりにくい・遅い）は報告できるが、
     価値の欠落は報告できない。** 摩擦の指摘だけを食べ続けると、全ての改善が
     「無難な道具としての完成度」に向かう（前作の実例: 30回以上の改善が全部そうだった）。
     フィードバック対応のたびに「コアの価値の語彙でこの問題を解けないか」を一度通す
   - 定期的に（数リビジョンごと）主要機能を棚卸しして、コアの価値に照らすレビューを行い、
     結果を台帳に記録する

### 検証の原則

1. **実インターフェース主義** — 検証は成果物の実インターフェース（実際の UI 操作・CLI 呼び出し・
   公開 API）だけを通して行う。内部関数の直呼び・内部状態の直書きで「動いた」ことにしない
   （内部状態の**読み取り**はアサーション用に可）。内部を呼ぶテストは全緑でも
   「利用者がその結果に到達できるか」を何も証明しない
   （前作の実例: 入力の横取りによる進行不能を、内部関数直呼びのテストが長期間素通しした）。
   例外は**テスト入力の注入**（保存データ・乱数シード・設定ファイル等）のみ——
   「入力データ」であり、内部状態の直接書き換えとは区別する
2. **通しシナリオ＝到達可能性の証明** — 主要な流れ（ユーザーが価値に到達する経路）を
   実インターフェースで最初から最後まで通すシナリオを常備する。
   振る舞い・進行を追加・変更したら、そこに実操作で到達するステップを同じコミットで足す
3. **非同期の安定待ち** — 遅延発火・アニメーション・非同期処理があるなら、テストは
   「安定状態が一定時間続く」まで待ってから次の操作をする（固定 sleep はフレークの温床）
4. **実行環境マトリクス** — 利用者が実際に使う環境（画面サイズ・OS・ランタイムのバージョン等）
   を列挙し、検証は全環境で回す。環境依存の破れは該当環境でしか検出できない
   （前作の実例: 縦持ちだけ直して横持ちの表示崩れを見逃した）
5. **到達可能性≠発見可能性** — 正解を知っているテストは「利用者が答えに気づけるか・
   使い方が分かるか」を検証しない。UX・判読性・ドキュメントの分かりやすさは、
   実寸・実環境での目視／実使用レビューで別途確認する

### 状態・データの原則

6. **重要な状態は「操作の瞬間に確定」か「読み込み時に逆算修復」** — 状態の確定・保存を
   遅延タイマーや演出・非同期のコールバック内に置くと、その窓で中断されたとき
   中途半端な永続データが焼き付く（前作の実例: 確定が演出コールバック内にあり、
   詰む中間セーブ窓が5箇所あった）。原因となる操作の瞬間に同期的に確定するか、
   やむを得なければ読み込み時の逆算修復（reconcile）を必ず対で実装し、復旧の検証ケースを足す。
   修復では「導出できる値（再計算してよい）」と「操作で確定した値（補完のみ・取り消し禁止）」を区別する
7. **入力・操作の対象を曖昧にしない** — 利用者の1操作が何に作用するかを常に一意にする
   （UI ならクリック対象の重なり・遮蔽、CLI なら曖昧なフラグ・破壊的操作の暗黙対象、
   API なら同名の多義エンドポイント）。曖昧さは構造で断ち、可能なら自動検証（invariant）で恒久検出する
8. **遅延実行は発火時に前提を再検証する** — setTimeout・キュー・ジョブなど遅延して走る処理は、
   発火時に前提（状態・画面・文脈）がまだ成立しているかガードしてから実行する
   （利用者は待ってくれない——その間に状態は変わる）。状態依存の決定は1関数に一元化し、
   通常経路と復帰経路の双方で同じ関数を使う

### インターフェースの原則

9. **利用者は製品世界のルールで操作を予測する——世界律の特例を作らない** — 製品が世界を
   物理（あるいは一貫した比喩・文法）で提示するなら、操作の結果もその律で解決する。
   実装都合のショートカット（近接スナップ・暗黙のオートコレクト・特定座標だけ効く操作等）は
   利用者の自然な予測を裏切り、「正しい操作をしたのに何も起きない」という摩擦報告として現れる
   （本作の実例: 皿の真上から落としても載らなかった——子どもは世界を物理として信じていた。
   リリース＝常に落下体・皿が捕捉、という統一物理で解決）。この種の報告を受けたら
   個別の判定緩和ではなく**世界律の一貫性の破れ**を疑う。
   - **補記1（目標の物理）**: 世界律は操作だけでなく**利用者が持ち込む目標**にも適用される。
     利用者が製品の語彙で自然に立てた目標（本作の実例: 同じ数にしたい・動物に食べさせたい）に
     製品が応答しないのは、操作が無視されるのと同じ破れ（v0.4 で自由遊び化により解決）
   - **補記2（応答のインフレ防止）**: 応答を増やすときは、その製品の**意味体系に閉じた応答**
     だけを許す（本作では「数量の演算であること」がゲート）。何にでも反応する世界は
     何も意味しなくなる——応答の追加要求はまずこのゲートを通す

## コア価値の棚卸し記録（原則0の定点観測）

| rev | 観測 | 判定 |
|---|---|---|
| v0.2 後の実プレイ | 定点質問「世界が答える手応えを感じたか」→ 保護者回答「ちゃんと感覚あったよ」。子どもは喜んで遊び続けている | **コア価値成立を確認**（統一落下物理の方向は正しかった）。残る指摘「どうしたらいいか分からない場面」は、てんびんに機能を足すのではなく第2ゲーム（わけっこ）の核として翻訳（v0.3） |
| v0.3 後の実プレイ | ①わけっこ「簡単すぎ・やらされてる感・すぐ飽きた」②てんびんで子どもは「同じ数にしたい」のに終われず辛がり、動物に直接食べさせようとして応答がなくがっかり ③タイトル/メニューが判別不能・文字解禁の指示 ④「創造性に応えるもの・知育が隠れてるもの」 | **わけっこは失敗と判定し引退**（思考泡＝答えの提示が「動詞が先」に構造的に違反。分配の学びは自由遊びの「食べさせて同じにする」に吸収）。却下案=動的空腹への手直し（突き詰めると自由遊び案に収束し、同じ動詞のゲーム2枚はメニュー混乱を悪化させるため）。てんびんは v0.4 で自由遊び化——原則9を「目標の物理」へ拡張し、応答の無際限化は**数量演算ゲート**（仕様 v0.4 §1）で防ぐ。台帳#9 で「動物への投下→地面に落とす」と修正した箇所は、世界律の破れは塞いだが**応答の空白**を残していた——子どもが実プレイでその空白を正確に突いた。「動物が欲しい物を示す」素材は将来「世界からのお願い」として復活可能性あり（棚卸しで再検討） |
| v0.6 後の実プレイ | 「どちらのゲームも、息子は回数の最後（ドット5つ）までやる前に飽きている。多分新たなチャレンジとか新鮮さがないからだ」 | advisor 相談の結論: 根本原因は構造の反復ではなく**発見の枯渇**——核の学び（傾き＝比較・あふれ＝容量）がラウンド1〜2で出尽くし、以降は同じ演算の反復になる。処方は装飾の鮮度でなく**新しい数量現象の投入** → **おおもの**（重さ2の果物）を採用（v0.7）: てんびんに「個数≠重さ」、おふねに「非自明なあまり」という第二の発見を、ラウンド2で確定初遭遇→以降漸増の曲線で投入。却下案: 世界からのお願い（ドット泡＝外部提示の目標。引退済みの思考泡と同型）、テーマ/時間帯変化（背景であり発見の枯渇に効かない）。実プレイ定点: おおもの初遭遇の発話が出るか・ドット5つまで遊び続けるか・ラウンド3以降の durationMs が減衰しないか |

## バグ台帳（このプロジェクトで見つけたもの）

| # | rev | 症状 | 根本原因 | 恒久対策（実装済み） |
|---|---|---|---|---|
| 1 | v0.1 初回実装 | 起動直後の一瞬、皿へのドロップが皿外判定になり左上へ落下（E2Eがフレークとして検出） | 皿座標が毎フレームの `computePlates()` で導出され、init〜最初の物理ステップの窓では公開読み取り面・入力判定が未初期化値 (0,0) を参照していた | `startProblem()` で皿座標を同期的に確定。ハーネス spring テスト冒頭の「公開直後の最初のスナップショットで皿座標が有効」アサーションで恒久検出 |
| 2 | v0.1 初回実装 | 親メニューの「ログを消去」直後にセッションが1件残る（消去になっていない） | `clearLogs()` が消去直後に `startNewSession()` で新レコードを push・永続化していた | clear は消去のみに変更。進行中セッションはストレージから切り離し、以後の persist で復活しない。ハーネス parent menu テストの「消去後 length 0」で恒久検出 |
| 3 | v0.1 初回実装 | 天秤のオーバーシュートが 1.7%＝目に見えず、「1〜2回ゆらっと揺れる」コアの触感が消失（検証は通るのに価値が欠ける典型） | 減衰係数 `SPRING_C=15`（ζ≈0.79）がコード内コメントの意図（ζ≈0.42）と乖離。数値だけ見て「揺れすぎ」を過剰に抑えた | `SPRING_C=8`（ζ≈0.42、初回オーバーシュート約23%）。ハーネス spring テストが (angle−target) の符号反転回数≥2 を検証し、触感の下限を自動検出可能な形に翻訳 |
| 4 | v0.1 監査 | 完了演出中に皿のりんごをつまむと TypeError → rAF ループ恒久停止＝全フリーズ（進行不能）。遷移中つかみ→旧りんご持ち越し（id衝突）でも同型クラッシュ | `handlePointerDown` が phase を見ずにドラッグ許可。食べキューは演出開始時の plateOrder のコピーで、つままれて `plate:null` になった id を後から処理して `plateOrder[null].indexOf` で死ぬ。例外1つで rAF ループ全体が死ぬ構造 | ①play 以外はドラッグ開始不可（入力ゲート）②`eatApple` は発火時に `state==='plate'` を再検証 ③`removeFromPlate` null 安全 ④`startProblem` で `dragApple` 解放＋id セッション通し一意 ⑤frame の try/catch（console.error 経由でハーネスが検出するので欠陥は隠れない）。テスト: grab during celebration |
| 5 | v0.1 監査 | セッション終了画面で左上長押ししてもメニューが開かず、触った瞬間に新セッションが始まる（親がログを見られない・空セッションで統計汚染）。縦持ちブロック中のタップでも不可視リスタート | `handlePointerDown` の判定順が「sessionEnd リスタート → 縦持ちブロック → 隅長押し」の順だった。隅ゾーンとりんご判定円の重なりも作用先を曖昧にしていた | 判定順を「メニュー → 縦持ちブロック → 隅長押し → sessionEnd リスタート → (play のみ)ドラッグ」に。テスト: sessionEnd corner long-press |
| 6 | v0.1 監査 | `tenbin_logs` に非オブジェクト要素（`[null]` 等）があると、起動は通るのに親メニューを開いた瞬間 TypeError で全フリーズ | `loadLogs` が「配列であること」しか検証せず、`computeStats` が要素をオブジェクト前提で読む | `loadLogs` で非オブジェクト要素をフィルタ＋`computeStats` に要素ガード。テスト: corrupted tenbin_logs |
| 7 | v0.2 実プレイ | 「つまんで皿の近くまで運ばないと載らない。上から落としても載ってほしい」（テスター指摘） | 皿中心120pxリリース＝即スナップという **UI 規約が「世界が答える」物理の一貫性を破っていた**。子どもは世界を物理として信じて操作する（→原則9に一般化） | 統一落下物理: リリースは常に落下体になり、落下中に皿が捕捉。dropOutside は物理の結果が出た瞬間（地面静止）にカウント。テスト: drop from above |
| 8 | v0.2 実プレイ | 「どうしたらOKなのか・何を考えさせるのかの手がかりがない」（テスター指摘） | 発見可能性の欠如（原則5）。文字禁止の制約下でオンボーディングの構造が未設計だった | 5秒無操作でゴーストハンドが「りんご→皿」の動詞を非言語デモ（タッチで即消える・状態を変えない）。「何を考えさせるか」は完了演出（下がった側が食べる）が担い、誘導過剰にしない。テスト: idle hint |
| 9 | v0.2 監査 | 動物に食べさせようと果物を放す・皿の真下の地面の果物をタップすると、落下せず皿へワープして載る（作用先の曖昧さ・世界律の破れ） | 「皿に重ねて放したら即着地」の判定帯に**下限がなく**、皿の真下〜地面までの縦柱全体が皿扱い。判定がポインタ座標基準で果物の実位置と乖離もしていた | 即着地帯に下限（皿厚＋果物半径＋余白）を追加し、判定を果物の実位置基準に統一。テスト: release on animal below plate（修正前 red 確認済み） |
| 10 | v0.2 監査 | 最初の完了演出以降、BGM が恒久的に 1/3 音量のまま（ダッキング解除が効かない） | 解除の `applyBgmGain()` が phase 更新**前**に呼ばれ、導出値が旧状態から計算されていた（呼び出し順への依存） | BGM 音量を毎ステップ状態から導出（目標値キャッシュで冪等）。phase 遷移との順序依存を構造で排除。聴感項目のため自動検証はなし——同クラス（状態確定前の導出値計算）は設計工程で見る |
| 11 | v0.2 監査 | （潜在）touchend ハンドラ内で例外が出ると `activePointerId` が残留し、以後の全タッチが無視される＝進行不能クラスの構造リスク | ポインタ解放がハンドラ実行**後**にあり、例外で到達しない | 解放をハンドラ呼び出しの前に移動（touchend/mouseup とも）。現実の throw 経路は未発見のため防御実装のみ |
| 12 | v0.3 監査 | 演出中におうちで離脱すると、メニュー/タイトルに `phase='celebrate'` が漏れ残り BGM が 1/3 音量のまま。前ゲームのパーティクルも凍結して次の入場直後に再生される | `goHome()` のリセット対象に `phase`/`celebrationType`/`particles`/`animalBounce` が含まれていなかった（画面遷移＝状態のライフサイクル境界、の列挙漏れ） | `goHome()` で演出系状態も全てリセット。テスト: goHome during celebration（修正前 red 確認済み） |
| 13 | v0.3 監査 | 右ゾーン右上の果物は、タッチ円がおうちボタン領域と重なり、少し上をタップすると home 押し込みに取られてつかめない（作用先の重なり。回避可能なので軽微） | 果物配置のクランプが HOME_ZONE のタッチ余白を考慮していなかった | 配置クランプの右上限を `HOME_ZONE.x − APPLE_TOUCH_R` に狭め、重なりを構造的に排除 |
| 14 | v0.4 監査 | 動物の**足元**に果物を放すと食べずに転がる（体の中心なら食べる——境界が体の内部を横切る）。「拒食に見える応答の空白」が帯状に残っていた（#9・v0.3実プレイと同クラス） | 食べさせ捕捉域が口中心の**円**で、動物の体の下半分をカバーしていなかった。子どもは「口」ではなく「動物」に渡す | 捕捉域を口から足元までの**縦カプセル**に変更（水平張り出し不変＝皿帯との非重複 invariant 維持）。テスト: feed at animal feet（修正前 red 確認済み） |
| 15 | v0.4 監査 | ロールイン中（未着手）に おうち離脱すると all-zero のラウンドレコードが記録され、親メニューの「ラウンド数」が実態より膨らむ | transition 開始と同時に次ラウンドが生成され、goHome が「未記録なら無条件 push」だった | goHome の記録条件に「操作痕があるか」を追加（fed/drop/removed/祝福/皿上個数のいずれか>0）。テスト: home during transition（修正前 red 確認済み） |
| 16 | v0.4 監査 | 破損した v2 ログの数値フィールドが文字列だと、親メニュー統計が文字列連結（「たべさせ 03回」等）に崩れる | `loadLogs` の検証が「要素がオブジェクト」まで（#6 の対策のまま）で、v2 の入れ子（rounds）の型を見ていなかった | `loadLogs` で v2 rounds の数値・真偽フィールドを読み込み時に正規化。テスト: corrupt v2 numeric fields（修正前 red 確認済み） |
| 17 | おふね監査 | 舟をゆっくり水中へ運んで静かに離すと、'dock' 状態のまま川の真ん中に恒久停止（クリティカル定義「舟が水中で停止したまま戻らない」に該当。つかみ直せるので完全な進行不能ではないが「何も起きない」応答の空白） | 弱リリース（速度<閾値）の分岐が `vx=0` で return するだけで、'dock' を動かす更新分岐が存在しなかった。「dock＝桟橋にいる」という前提と「dock＝出航していない」という実装の乖離 | 弱リリース時、桟橋から動かされていれば 'returning'（流れの追従＝必ず桟橋へ収束）に遷移。テスト: slow release mid-water（修正前 red 確認済み。フリングだけのテストはこの窓を素通しした——「弱い操作」も検証対象にする） |
| 18 | おふね監査 | 荷下ろし中（静止して見える舟）の甲板に果物を放すと、実体のある舟をすり抜けて水に落ちる（#7/#14 と同クラスの世界律の破れ） | 甲板捕捉が 'dock' 限定だった。「静止している＝載れる」という子どもの物理予測と実装状態の乖離 | 捕捉条件を「静止している舟（dock/unloading）」に拡大（追い積みは荷下ろしループが自然に処理）。移動中は載らない＝世界律として一貫。テスト: load onto unloading boat（修正前 red 確認済み） |
| 19 | おふね監査 | v/game フィールド欠損の破損ログレコードが数値正規化をすり抜け、統計が文字列連結に崩れる（#16 の再発。正規化ゲートの自己申告依存） | 正規化の適用条件がレコードの自己申告（`s.v === 1 && s.game === 'ofune'`）で、破損＝自己申告が壊れているデータほど素通しする構造 | **正規化は「どのキーから読んだか」で決める**（自己申告に依存しない）＋読み手側（統計集計）でも Number() 化の二重防衛。テスト: legacy logs の v 欠損レコード（修正前 red 確認済み） |
| 20 | v0.6 実プレイ | 「一度船を動かした後、果物が乗せられないことがある」（ユーザー指摘）——ほぼ帰着して静止に見える舟に置いた果物が水へ落ちる | 帰航は指数追従で桟橋へ漸近するため「見た目静止・状態は returning」の窓が数秒あり、甲板捕捉が状態ラベル（dock/unloading）でゲートされていた（#17 の対策が生んだ隣接欠陥。「止まって見える＝載れる」という子どもの物理予測と状態機械の乖離、#18 と同クラス） | 捕捉条件を実速度判定 `boatIsStationary()`（dock/unloading または returning かつ lastSpeed<30px/s）に変更。テスト: quasi-stationary returning boat（修正前 red 確認済み） |
| 21 | v0.6 実プレイ | 「ふねとくだもの、つかみたいのがつかめない」（ユーザー指摘）——帰航・出航中の舟が掴めない。甲板の果物を取り戻せない（てんびんの可逆性と非対称）。船体タッチが積み荷の広いタッチ円に奪われる | 舟のつかみが `state==='dock'` 限定。甲板果物（state 'boat'）が findDraggableApple の対象外。優先判定が果物一律62pxで船体と重なると作用先が曖昧 | 'held' 状態を導入し unloading 以外いつでも掴める（慣性は手で消える）。dock 中の甲板果物は個別つまみ可（36pxの狭判定→load同期→支え再計算）。優先順位＝積み荷(36px)→果物(62px)→船体。unloadNextFruit は消費時に state を再検証（#4 と同クラスの防御）。テスト: returning boat held / take back off deck / hull grabs boat（修正前 red 確認済み） |
| 22 | v0.6 実プレイ | 「果物を置いたところじゃない所に整列されるのが嫌」（ユーザー指摘）——載せるたび全果物がグリッド座標へテレポート | UI 都合の自動整列が「子どもが置いた」という操作の結果を上書きしていた（#7 と同クラス: UI 規約が「世界が答える」物理の一貫性を破る） | 置いた水平位置を保持（端のみクランプ）し、縦は支えの物理（重なりがあれば上に積む）で解決。下ろすときは支えを失った果物だけ沈下し、残りは並べ直さない。テスト: plate/deck place keeps spot・removal does not rearrange（修正前 red 確認済み） |
| 23 | v0.6 監査 | 帰航のほぼ静止窓では「載せられるのに取り戻せない」——いま載せた甲板の果物をタップすると舟本体が持ち上がる（作用先のすり替え） | #20 の対策（載せる側だけ実速度判定に）が非対称で、取り戻す側は `state==='dock'` ラベル限定のままだった | 「載る」と「取れる」を同じ述語 `boatIsStationary()` で判定（unloading のみ不可）。状態依存の決定は1関数に一元化（原則8）。テスト: take back while quasi-stationary（修正前 red 確認済み） |
| 24 | v0.6 監査 | 「積んで取り戻すだけ」のラウンドが、おうち離脱で未記録になる（着手済みなのに未着手扱い） | #15 の「操作痕」判定がカウンタ合計のみで、カウンタに現れない操作（積む→取り戻す等）を「触っていない」と誤判定した——**対策の判定条件が、後から増えた操作の種類に追従していなかった** | 操作痕に `firstTouchSimMs !== null`（何かを一度でもつまんだ事実）を追加（てんびん側も対称に）。テスト: load+take-back only round recorded on home（修正前 red 確認済み） |
| 25 | v0.6 監査 | 高いスタックに上から落とした果物が、描かれているスタックをすり抜けてから頂上へ約100pxテレポートする（世界律の破れ。#7 と同クラス） | 捕捉面が固定（皿面/甲板線）のままで、v0.6 の積み重ねによる着地位置の上昇を追わなかった | 捕捉面をスタック考慮で持ち上げ（着地予定位置から皿面と同じ間合い）。**ただし面を「ずらす」と元の面との間に不感帯ができて既存操作がすり抜ける**（修正中にハーネスが検出）——帯は上端だけ広げ、落下捕捉は「持ち上げた面か元の面のどちらかの通過」にする。テスト: falling fruit lands ON the stack（修正前 red 確認済み・実測98.5pxのテレポートを検出） |
| 26 | v0.6 監査 | （潜在）ウィンドウ外で mouseup を失うと `activePointerId='mouse'` が残留し、以後の全入力（タッチ含む）が無視される＝進行不能クラスの構造リスク（#11 の残り半分） | mousedown が残留 id を無条件に弾く構造で、自己回復の経路が canvas 上の mouseup しかなかった | mousedown 時に残留を検出したら先に解放（handlePointerUp）してから新しい操作を受ける。実 iPad は touchcancel が担うため防御実装のみ（#11 と同じ流儀） |
| 27 | v0.7 実プレイ | 「船の上に果物が乗せられる高さの上限みたいなのがある」（ユーザー指摘）——甲板の約90pxより上で放すと必ず水没。容量あふれかと疑ったが実測で幾何のバグと判明 | リリース速度の推定（`computeDragVelocity`）が「動かした間」の履歴サンプルだけで傾きを取るため、**止めて狙ってから放しても古いスワイプ速度で射出**される。高い位置ほど滞空時間が長く、横流れの蓄積が甲板捕捉半幅(±105px)を超えて水没＝「高さの上限」に見えた（落下トレースで x=430→536 のドリフトを実測） | 世界律「手を止めてから放したら、飛ばない」（原則9）: 最終移動から180ms（シミュ時間）超で速度ゼロ＋分母を「履歴先頭〜今」に伸ばして保持時間で自然減衰。両ゲーム・舟リリース共通。テスト: aim-and-release from high above deck（修正前は診断スクリプトで holdY≤400 全滅を確認） |
| 28 | v0.7 監査 | おおものだけ「縁が動物・船体に触れているのに応答しない」細帯（口: 約13px、甲板: 約9px。#14 と同クラスの応答の空白） | 口カプセル・甲板捕捉半幅が果物の実半径でなく通常半径（APPLE_R）固定だった | `mouthContains`/`findMouthAt` に実半径を渡し、甲板半幅を `+（r−APPLE_R）` 拡張。**皿の半幅は据え置き**（広げると口ゾーンと12px重なり、上から落とす操作を皿が横取りする #9 が再発——おおものが皿を4pxかすめて落ちるのは作用先の一意性を優先して許容）。幾何 invariant はおおもの半径込みで検証に強化。テスト: 動物のわき腹への縁タッチ給餌・船体+110px での縁積載（修正前 red 確認済み） |
| 29 | v0.7 監査 | おおもの同士が最大20pxめり込んで出現しうる。HOME ゾーンとの非重複も「座標の偶然の1px」でしか成立していなかった（#13 の対策の実質喪失） | 初期配置の間隔・クランプ計算が通常半径で走った**後に**おおもの置き換えで r を拡大していた（導出の順序と前提のずれ） | おおもの適用後に実半径ベースの再リラックス＋タッチ円（実r）基準の HOME クランプを追加（`relaxApplesForRadius`）。ハーネスにタッチ円×HOME 非重複の invariant アサーションを追加 |

## 追記ルール

- 1バグ = 1行。**症状 → 根本原因 → 対策** をこの順で。対策は「どのテスト／invariant に実装したか」まで書く
- 修正だけで済ませず、「同じクラスのバグが再発したら検証が検出できるか？」を自問する。
  できないなら検証を足す
- 原則として一般化できる学びは「設計原則」の節にも番号を振って足す（→ 足したら `/backflow` で正本へ）
- **肥大化したらアーカイブ**（ゲーム系譜の台帳運用から一般化）: バグ台帳が30行を超えたら
  （または全体が300行を超えたら）、対策がコード化済み（テスト／invariant に実装済み）の
  古い行から順に `KNOWLEDGE-archive.md` へ移す。本体には「#番号｜一行要約」の索引を残す。
  設計原則は生きた知識なので、原則はアーカイブせず本体に残し続ける

## サブエージェント運用の雛形

クリティカル欠陥の定義（CLAUDE.md）に関わる変更では、実装とは別の文脈で監査を回す：

- **独立監査**（auditor）: 「CLAUDE.md のクリティカル欠陥の定義を監査観点に、該当する経路を
  逆向きに辿って成立条件を検証。遅延実行×状態遷移の競合、中断・復帰で壊れる永続データ、
  入力対象の曖昧さを重点確認。コード引用で根拠を示す。編集禁止」
- **検証実行**（verifier）: 検証コマンドを実行し、fail/warn を要約させる
- 監査所見は鵜呑みにせず、検証で再現できたものだけをバグとして扱う
  （再現できたら台帳へ、テスト／invariant へ反映）

## 既知の制約

- **緑＝正しさではない。** 検証は到達可能性・回帰を測るが、コアの価値・使いやすさは測れない。
  測れないものは検証ではなく設計工程（原則0）で守る
- このテンプレートは検証の実体を持たない。検証コマンドが未整備の間は、変更パイプラインの
  第3工程が空洞になる——**立ち上げ直後に検証の器を作ることが最優先タスク**
